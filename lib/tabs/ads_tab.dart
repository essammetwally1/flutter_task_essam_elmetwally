import 'package:flutter/material.dart';

class AdsTab extends StatelessWidget {
  const AdsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Ads Tab'));
  }
}
