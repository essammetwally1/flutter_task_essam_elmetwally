import 'package:flutter/material.dart';

class AddadsTab extends StatelessWidget {
  const AddadsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('AddAds Tab'));
  }
}
